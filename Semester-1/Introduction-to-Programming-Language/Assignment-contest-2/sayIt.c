#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
    {
        // print index with a dot
        printf("%d. I Want More Assignments\n", i);
    }

    return 0;
}